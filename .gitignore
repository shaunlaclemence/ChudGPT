# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets & local state — never commit keys
.env
.env.*
!.env.example
keys.json
*.tokens.json
state.json
secrets.json

# local quota tracking
.chudgpt/

# per-run test output, one file per run and never pruned
tests/outputs/
